const $=s=>document.querySelector(s);let pois=[];const api=async u=>{const r=await fetch(u);const d=await r.json();if(!r.ok)throw new Error(d.error||'Anfrage fehlgeschlagen');return d};const el=(t,v)=>{const x=document.createElement(t);if(v!==undefined)x.textContent=v;return x};
function draw(items){const svg=$('#map');svg.replaceChildren();if(!items.length)return;const lons=items.map(x=>x.lon),lats=items.map(x=>x.lat),minX=Math.min(...lons),maxX=Math.max(...lons),minY=Math.min(...lats),maxY=Math.max(...lats),spanX=Math.max(.01,maxX-minX),spanY=Math.max(.01,maxY-minY);items.forEach(p=>{const c=document.createElementNS('http://www.w3.org/2000/svg','circle');c.setAttribute('cx',35+(p.lon-minX)/spanX*610);c.setAttribute('cy',385-(p.lat-minY)/spanY*350);c.setAttribute('r','7');c.setAttribute('class','point');const title=document.createElementNS('http://www.w3.org/2000/svg','title');title.textContent=p.name;c.append(title);svg.append(c)})}
function render(items){pois=items;$('#count').textContent=`${items.length} Treffer`;const box=$('#results');box.replaceChildren();items.forEach(p=>{const d=el('article');d.className='poi';const a=el('a','In OpenStreetMap öffnen');a.href=`https://www.openstreetmap.org/?mlat=${p.lat}&mlon=${p.lon}#map=16/${p.lat}/${p.lon}`;a.target='_blank';a.rel='noopener';d.append(el('strong',p.name),el('small',`${p.category} · ${p.city}`),a);box.append(d)});draw(items)}
async function search(query){const d=await api('/api/search?q='+encodeURIComponent(query));render(d.results)}$('#search').onsubmit=e=>{e.preventDefault();search($('#query').value).catch(e=>alert(e.message))};$('#all').onclick=()=>{ $('#query').value='';search('').catch(e=>alert(e.message))};api('/api/status').then(d=>$('#status').textContent=`${d.pois} POIs · ${d.source} · ${d.read_only?'schreibgeschützt':'beschreibbar'}`).catch(()=>{});search('München').catch(e=>alert(e.message));
