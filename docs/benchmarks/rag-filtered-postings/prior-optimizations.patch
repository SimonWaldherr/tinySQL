diff --git a/internal/engine/exec_cte.go b/internal/engine/exec_cte.go
index df9ab4b..df67e51 100644
--- a/internal/engine/exec_cte.go
+++ b/internal/engine/exec_cte.go
@@ -7,0 +8 @@ import (
+	"slices"
@@ -113,7 +114,15 @@ func alignRecursiveCTERows(accRs *ResultSet, nextRs *ResultSet, cteName string)
-	sources := make([]string, len(accRs.Cols))
-	targets := make([]string, len(accRs.Cols))
-	qualified := make([]string, len(accRs.Cols))
-	for i, col := range accRs.Cols {
-		sources[i] = strings.ToLower(nextRs.Cols[i])
-		targets[i] = strings.ToLower(col)
-		qualified[i] = strings.ToLower(cteName + "." + targets[i])
+	return newRecursiveCTEAlignment(accRs.Cols, nextRs.Cols, cteName).align(nextRs.Rows)
+}
+
+// Each recursive frontier uses the same output schema in the common case.
+// Keep its positional mapping local to this execution, never in the shared AST.
+type recursiveCTEAlignment struct {
+	columns, sources, targets, qualified []string
+}
+
+func newRecursiveCTEAlignment(anchor, next []string, cteName string) *recursiveCTEAlignment {
+	a := &recursiveCTEAlignment{
+		columns:   slices.Clone(next),
+		sources:   make([]string, len(anchor)),
+		targets:   make([]string, len(anchor)),
+		qualified: make([]string, len(anchor)),
@@ -121,4 +130,13 @@ func alignRecursiveCTERows(accRs *ResultSet, nextRs *ResultSet, cteName string)
-	alignedRows := make([]Row, len(nextRs.Rows))
-	for rowIdx, r := range nextRs.Rows {
-		nr := make(Row, len(targets)*2)
-		for i, source := range sources {
+	for i, col := range anchor {
+		a.sources[i] = strings.ToLower(next[i])
+		a.targets[i] = strings.ToLower(col)
+		a.qualified[i] = strings.ToLower(cteName + "." + col)
+	}
+	return a
+}
+
+func (a *recursiveCTEAlignment) align(rows []Row) []Row {
+	alignedRows := make([]Row, len(rows))
+	for rowIdx, r := range rows {
+		nr := make(Row, len(a.targets)*2)
+		for i, source := range a.sources {
@@ -127 +145 @@ func alignRecursiveCTERows(accRs *ResultSet, nextRs *ResultSet, cteName string)
-				val = r[nextRs.Cols[i]]
+				val = r[a.columns[i]]
@@ -129,2 +147,2 @@ func alignRecursiveCTERows(accRs *ResultSet, nextRs *ResultSet, cteName string)
-			nr[targets[i]] = val
-			nr[qualified[i]] = val
+			nr[a.targets[i]] = val
+			nr[a.qualified[i]] = val
@@ -207,0 +226,2 @@ func evalRecursiveCTE(env ExecEnv, cte *CTE) (*ResultSet, error) {
+	var alignment *recursiveCTEAlignment
+	cteKey := strings.ToLower(cte.Name)
@@ -213 +233 @@ func evalRecursiveCTE(env ExecEnv, cte *CTE) (*ResultSet, error) {
-		env.ctes[strings.ToLower(cte.Name)] = &ResultSet{Cols: accRs.Cols, Rows: frontier}
+		env.ctes[cteKey] = &ResultSet{Cols: accRs.Cols, Rows: frontier}
@@ -226 +246,4 @@ func evalRecursiveCTE(env ExecEnv, cte *CTE) (*ResultSet, error) {
-		alignedRows := alignRecursiveCTERows(accRs, nextRs, cte.Name)
+		if alignment == nil || !slices.Equal(alignment.columns, nextRs.Cols) {
+			alignment = newRecursiveCTEAlignment(accRs.Cols, nextRs.Cols, cte.Name)
+		}
+		alignedRows := alignment.align(nextRs.Rows)
diff --git a/internal/engine/triggers.go b/internal/engine/triggers.go
index 249e2dd..5b6f30e 100644
--- a/internal/engine/triggers.go
+++ b/internal/engine/triggers.go
@@ -134,0 +135 @@ type triggerListRunner struct {
+	binding  triggerRowBinding
@@ -172 +173,3 @@ func (r *triggerListRunner) fire(env ExecEnv, newRow Row, oldRow Row) error {
-	binding := triggerRowBinding{newRow: newRow, oldRow: oldRow}
+	// Execution is synchronous and nested DML owns a separate runner. Reuse
+	// this statement-local binding rather than escaping a new object per row.
+	r.binding = triggerRowBinding{newRow: newRow, oldRow: oldRow}
@@ -175 +178 @@ func (r *triggerListRunner) fire(env ExecEnv, newRow Row, oldRow Row) error {
-		if err := executePreparedTrigger(env, program, &binding); err != nil {
+		if err := executePreparedTrigger(env, program, &r.binding); err != nil {
diff --git a/internal/storage/secondary_index_range.go b/internal/storage/secondary_index_range.go
index ece0df9..75f1488 100644
--- a/internal/storage/secondary_index_range.go
+++ b/internal/storage/secondary_index_range.go
@@ -76 +76,2 @@ func (t *Table) LookupSecondaryIndexRange(idx *SecondaryIndex, prefix []any, lo,
-	prefixKey := CanonicalIndexKey(prefix)
+	var prefixScratch [128]byte
+	prefixKey := canonicalIndexKeyInto(prefixScratch[:0], prefix)
@@ -80,0 +82 @@ func (t *Table) LookupSecondaryIndexRange(idx *SecondaryIndex, prefix []any, lo,
+	var loScratch, hiScratch [numericComponentLen]byte
@@ -82 +84 @@ func (t *Table) LookupSecondaryIndexRange(idx *SecondaryIndex, prefix []any, lo,
-		loEnc = appendCanonicalIndexValue(nil, lo.Value)
+		loEnc = appendCanonicalIndexValue(loScratch[:0], lo.Value)
@@ -88 +90 @@ func (t *Table) LookupSecondaryIndexRange(idx *SecondaryIndex, prefix []any, lo,
-		hiEnc = appendCanonicalIndexValue(nil, hi.Value)
+		hiEnc = appendCanonicalIndexValue(hiScratch[:0], hi.Value)
@@ -98 +100,2 @@ func (t *Table) LookupSecondaryIndexRange(idx *SecondaryIndex, prefix []any, lo,
-	seek := append(append([]byte(nil), prefixKey...), loEnc...)
+	var seekScratch [128 + numericComponentLen]byte
+	seek := append(append(seekScratch[:0], prefixKey...), loEnc...)
@@ -170 +173,2 @@ func IndexRangeComponentEncodable(v any) bool {
-	return len(appendCanonicalIndexValue(nil, v)) == numericComponentLen
+	var scratch [numericComponentLen]byte
+	return len(appendCanonicalIndexValue(scratch[:0], v)) == numericComponentLen
diff --git a/internal/storage/wal_advanced.go b/internal/storage/wal_advanced.go
index 5f5feb4..89522f9 100644
--- a/internal/storage/wal_advanced.go
+++ b/internal/storage/wal_advanced.go
@@ -1246,27 +1246,18 @@ func (w *AdvancedWAL) calculateChecksum(record *WALRecord) uint32 {
-	h := crc32.New(walCRCTable)
-	var b [8]byte
-	writeU64 := func(v uint64) {
-		binary.LittleEndian.PutUint64(b[:], v)
-		_, _ = h.Write(b[:])
-	}
-	writeU64(uint64(record.LSN))
-	writeU64(uint64(record.TxID))
-	_, _ = h.Write([]byte{byte(record.OpType)})
-	_, _ = io.WriteString(h, record.Tenant)
-	_, _ = h.Write([]byte{0})
-	_, _ = io.WriteString(h, record.Table)
-	_, _ = h.Write([]byte{0})
-	writeU64(uint64(record.RowID))
-	writeU64(uint64(record.Timestamp.UnixNano()))
-	// scratch is reused across every value/column below instead of each
-	// hashWALValue call declaring its own local buffer: a buffer whose slice
-	// is handed to h.Write (an interface method) escapes to the heap, so one
-	// shared, heap-allocated-once buffer beats one fresh escape per value.
-	var scratch [40]byte
-	hashWALImage(h, record.BeforeImage, &scratch)
-	hashWALImage(h, record.AfterImage, &scratch)
-	// One buffer + one Write per column instead of five separate calls
-	// (three of them single-byte literals, one a dynamic-string conversion):
-	// each separate call to h.Write on the interface is a distinct potential
-	// heap escape, whereas building "c<name>;<type>;" once in scratch and
-	// writing it in a single call amortizes to the one shared allocation.
+	h := &walChecksumWriter{}
+	// The fixed fields and names form one contiguous CRC fragment. Ordinary
+	// identifiers fit in the shared scratch; oversized names grow without truncation.
+	var scratch [64]byte
+	b := binary.LittleEndian.AppendUint64(scratch[:0], uint64(record.LSN))
+	b = binary.LittleEndian.AppendUint64(b, uint64(record.TxID))
+	b = append(b, byte(record.OpType))
+	b = append(b, record.Tenant...)
+	b = append(b, 0)
+	b = append(b, record.Table...)
+	b = append(b, 0)
+	b = binary.LittleEndian.AppendUint64(b, uint64(record.RowID))
+	b = binary.LittleEndian.AppendUint64(b, uint64(record.Timestamp.UnixNano()))
+	_, _ = h.Write(b)
+	// Header, row scalars and descriptors share one query-local allocation.
+	valueScratch := (*[40]byte)(scratch[:40])
+	hashWALImage(h, record.BeforeImage, valueScratch)
+	hashWALImage(h, record.AfterImage, valueScratch)
@@ -1281 +1272,16 @@ func (w *AdvancedWAL) calculateChecksum(record *WALRecord) uint32 {
-	return h.Sum32()
+	return h.crc
+}
+
+// Unlike hash/crc32's digest, this writer accepts strings without a temporary
+// byte-slice allocation. Update consumes the bytes synchronously and retains
+// none of them. The checksum stream and durable format remain unchanged.
+type walChecksumWriter struct{ crc uint32 }
+
+func (h *walChecksumWriter) Write(p []byte) (int, error) {
+	h.crc = crc32.Update(h.crc, walCRCTable, p)
+	return len(p), nil
+}
+
+func (h *walChecksumWriter) WriteString(s string) (int, error) {
+	h.crc = crc32.Update(h.crc, walCRCTable, []byte(s))
+	return len(s), nil
